require 'spec_helper'

RSpec.describe SixSaferpay::<%=name.capitalize %> do

subject { SpinningWheel.create('<%=name%>') }

  let(:hash) {
    {
    }
  }

  describe 'to_hash' do
    it 'returns the hash representation of the <%=name%>' do
      expect(subject.to_hash).to eq(hash)
    end
  end
end
